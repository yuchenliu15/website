<div class="post">
  <%= button_to '⬅️ back', posts_path, method: :get, class: "button" %>

  <p class="title">
    <%= @post.title %>
  </p>

  <p class="body">
    <%= @post.body %>
  </p>

  <% if session[:user_id] %>
    <%= link_to 'Edit', edit_post_path(@post) %>
  <% end %>
  <%= button_to '⬅️ back', posts_url, method: :get, class: "button" %>
</div>

